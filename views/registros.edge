<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sensor DHT11</title>
    <!-- Estilos de la pagina -->
    <style>
        *{
            padding: 0;
            margin: 0;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }

        .title{
            width: 100%;
            height: 20vh;
            background-color: #393E41;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .title h1{
            color: #FAFAFA;
            font-size: 19px;
            text-align: center;
        }

        .content{
            width: 100%;
            padding: 20px 0;
            background-color: #E7E5DF;
            display: flex;
            flex-direction: column;
            justify-content: space-around;
            align-items: center;
        }

        .table{
            width: 80%;
            text-align: center;
            border: 1px solid #393E41;
            background-color: #FAFAFA;
        }
        .table-head{
            border-bottom: 1px solid #393E41;
        }
        .table-body td{
            border-bottom: 1px solid #393E41;
        }
    </style>

</head>
<body>
    
    <div class="title">
        <h1>Registros</h1>
    </div>

    <div class="content">
        <table class="table">
            <thead>
                <tr>
                    <th>Temperatura</th>
                    <th>Fecha</th>
                </tr>
            </thead>
            <tbody class="table-body"> 
                <!-- Imprimimos una fila por cada registro en la base de datos -->
                @each(value in values)
                <tr>
                    <td>{{value.valor}}</td>
                    <td>{{value.date}}</td>
                </tr>
                @endeach
            </tbody>
        </table>
    </div>
    

</body>
</html>